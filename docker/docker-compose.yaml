version: "3.5"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - path_prefix=
        - geoserver=
    image: freeyeti/geoserver-tools:__COMMIT_TAG__